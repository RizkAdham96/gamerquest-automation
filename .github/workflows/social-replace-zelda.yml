name: Replace Zelda Instagram Carousel

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: gamerquest-social-publishing
  cancel-in-progress: false

jobs:
  replacement-closed:
    runs-on: ubuntu-latest
    steps:
      - name: Replacement already executed
        run: echo "The one-off Zelda replacement workflow is closed."
